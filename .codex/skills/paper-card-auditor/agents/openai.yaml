interface:
  display_name: "Paper Card Auditor"
  short_description: "Audit one paper card against the full paper and the current survey writing structure."
  default_prompt: "Audit one existing paper card against the full paper. Fix hallucinations, tighten outline alignment, and report section-level writing impact."

policy:
  allow_implicit_invocation: false
