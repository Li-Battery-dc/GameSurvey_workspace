interface:
  display_name: "Paper Card Auditor"
  short_description: "Finalize existing paper cards against full papers."
  default_prompt: "Use $paper-card-auditor to final-audit existing paper cards, fix unsupported claims, validate taxonomy labels, and sync finalized registry status."

policy:
  allow_implicit_invocation: false
